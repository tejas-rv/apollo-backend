spring:
  application:
    name: apollo-elevator-backend

  datasource:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: smtp.gmail.com
    port: 587
    username: tejas.v008@gmail.com
    password: vbabfoqyqeqxrwtz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 9071

app:
  master-key: /RcKGbKF3Che8FLiKq6cuPcsmTPg4ZwKgIXh111eQzI=
  datasource:
    primary:
      url: jdbc:postgresql://ep-ancient-voice-azj0gtoq-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      username: neondb_owner1234
      password: npg_FU5hYwtveqo3
    fallback:
      url: jdbc:postgresql://localhost:5432/apollo-elevator-development
      username: postgres
      password: Tumakuru@123
    connect-timeout-seconds: 5
  whatsapp:
    enabled: true
    base-url: ${WHATSAPP_BASE_URL:https://graph.facebook.com}
    api-version: ${WHATSAPP_API_VERSION:v20.0}
    phone-number-id: ${WHATSAPP_PHONE_NUMBER_ID:}
    access-token: ${WHATSAPP_ACCESS_TOKEN:}
  email:
    enabled: true
    from: tejas.v008@gmail.com
    admin-recipients:
      - tejasv.1si18te052@gmail.com
      - tejas.rv@outlook.com
#      - admin3@apolloelevator.in

logging:
  level:
    com.apollo.elevator: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

springdoc:
  api-docs:
    path: /v3/api-docs

  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true
    operations-sorter: method
    tags-sorter: alpha

  default-consumes-media-type: application/json
  default-produces-media-type: application/json
